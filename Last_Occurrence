//Linear search Last Occurrence problem
#include <iostream>

using namespace std;

int main() {
	int n, m, i, j;
	cin >> n >> m;
	int a[n];		
	for (i=0; i<n; i++)
		cin >> a[i];
  
	for (j=n-1; j>=0; j--)
	{
		if (a[j]==m)
			{cout << j+1;
			break;}
	}
	return 0;
}
